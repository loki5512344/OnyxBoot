image boot.img {
    hdimage {
    }

    partition bootloader {
        image = bootloader.bin
        offset = 0
        size = 32768
    }

    partition kernel {
        image = kernel.elf
        offset = 1M
        size = 8M
    }
}
